import { Typography } from 'antd'

const { Title } = Typography

export function SeriesPage() {
  return (
    <div>
      <Title level={2}>Series</Title>
      <p>Manage racing series.</p>
    </div>
  )
}
